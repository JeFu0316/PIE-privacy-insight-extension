# Build artifacts / packaged extension
dist/

# Local clone of the separate privacy-policy Pages repo (NOT inside the extension
# folder used for Load unpacked — Chrome rejects names starting with "_").
# Keep the clone at C:\PIE-privacy-site (sibling of PIE-dev), never under C:\PIE-dev.
